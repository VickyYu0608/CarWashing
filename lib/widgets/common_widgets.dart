/// Shared widgets extracted from main.dart (incremental refactor).
/// Import from main.dart for now — widgets still defined there for compatibility.
library;

export '../main.dart'
    show
        SectionTitle,
        TopBar,
        EmptyState,
        StatusPill,
        AppTextField,
        InfoChip;
